{
  "name": "workstation-kpi",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A super simple workstation KPI dashboard built with Bun, semantic HTML, BEM CSS, and functional JS.",
  "scripts": {
    "dev": "bun --watch server.ts",
    "start": "bun server.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write ."
  },
  "dependencies": {
    "systeminformation": "^5.27.7"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "^1.4.2",
    "typescript": "^7.0.2"
  }
}
